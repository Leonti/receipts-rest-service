language: scala
jdk: oraclejdk8
sudo: false

addons:
  apt:
    packages:
      - imagemagick

scala:
   - 2.11.8

services:
  - docker

branches:
  only:
    - master

script: sbt test && sbt integrate && sbt assembly && bash publish.sh
